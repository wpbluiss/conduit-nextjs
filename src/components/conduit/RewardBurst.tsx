// Re-export from the canonical location — src/components/ui/reward-burst.tsx
// Import from there for new call sites; this shim keeps existing imports working.
export { RewardBurst } from "@/components/ui/reward-burst";
export type { RewardBurstProps } from "@/components/ui/reward-burst";
